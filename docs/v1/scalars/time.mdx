---
id: time
title: Time
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

A time string with format: HH:mm:ss.SSS

```graphql
scalar Time
```
