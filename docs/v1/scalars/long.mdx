---
id: long
title: Long
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

The `Long` scalar type represents 52-bit integers

```graphql
scalar Long
```
