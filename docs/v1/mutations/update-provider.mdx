---
id: update-provider
title: updateProvider
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
updateProvider(
  input: updateProviderInput
): updateProviderPayload

```

### Arguments

#### `input` ([`updateProviderInput`](/v1/inputs/update-provider-input))

### Type

#### [`updateProviderPayload`](/v1/objects/update-provider-payload)
