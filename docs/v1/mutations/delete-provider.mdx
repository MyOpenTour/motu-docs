---
id: delete-provider
title: deleteProvider
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
deleteProvider(
  input: deleteProviderInput
): deleteProviderPayload

```

### Arguments

#### `input` ([`deleteProviderInput`](/v1/inputs/delete-provider-input))

### Type

#### [`deleteProviderPayload`](/v1/objects/delete-provider-payload)
