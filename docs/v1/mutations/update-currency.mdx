---
id: update-currency
title: updateCurrency
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
updateCurrency(
  input: updateCurrencyInput
): updateCurrencyPayload

```

### Arguments

#### `input` ([`updateCurrencyInput`](/v1/inputs/update-currency-input))

### Type

#### [`updateCurrencyPayload`](/v1/objects/update-currency-payload)
