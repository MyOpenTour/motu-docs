---
id: create-provider
title: createProvider
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
createProvider(
  input: createProviderInput
): createProviderPayload

```

### Arguments

#### `input` ([`createProviderInput`](/v1/inputs/create-provider-input))

### Type

#### [`createProviderPayload`](/v1/objects/create-provider-payload)
