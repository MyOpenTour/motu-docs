---
id: update-like
title: updateLike
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
updateLike(
  input: updateLikeInput
): updateLikePayload

```

### Arguments

#### `input` ([`updateLikeInput`](/v1/inputs/update-like-input))

### Type

#### [`updateLikePayload`](/v1/objects/update-like-payload)
