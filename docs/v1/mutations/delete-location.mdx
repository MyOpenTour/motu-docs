---
id: delete-location
title: deleteLocation
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
deleteLocation(
  input: deleteLocationInput
): deleteLocationPayload

```

### Arguments

#### `input` ([`deleteLocationInput`](/v1/inputs/delete-location-input))

### Type

#### [`deleteLocationPayload`](/v1/objects/delete-location-payload)
