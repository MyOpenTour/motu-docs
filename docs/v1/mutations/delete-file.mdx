---
id: delete-file
title: deleteFile
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Delete one file

```graphql
deleteFile(
  input: deleteFileInput
): deleteFilePayload

```

### Arguments

#### `input` ([`deleteFileInput`](/v1/inputs/delete-file-input))

### Type

#### [`deleteFilePayload`](/v1/objects/delete-file-payload)
