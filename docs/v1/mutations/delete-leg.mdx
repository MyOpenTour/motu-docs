---
id: delete-leg
title: deleteLeg
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
deleteLeg(
  input: deleteLegInput
): deleteLegPayload

```

### Arguments

#### `input` ([`deleteLegInput`](/v1/inputs/delete-leg-input))

### Type

#### [`deleteLegPayload`](/v1/objects/delete-leg-payload)
