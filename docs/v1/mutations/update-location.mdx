---
id: update-location
title: updateLocation
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
updateLocation(
  input: updateLocationInput
): updateLocationPayload

```

### Arguments

#### `input` ([`updateLocationInput`](/v1/inputs/update-location-input))

### Type

#### [`updateLocationPayload`](/v1/objects/update-location-payload)
