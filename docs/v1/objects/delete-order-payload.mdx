---
id: delete-order-payload
title: deleteOrderPayload
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type deleteOrderPayload {
  order: Order
}
```

### Fields

#### `order` ([`Order`](/v1/objects/order))
