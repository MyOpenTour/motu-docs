---
id: currency-connection-decimal-digits
title: CurrencyConnectionDecimal_digits
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type CurrencyConnectionDecimal_digits {
  key: Int
  connection: CurrencyConnection
}
```

### Fields

#### `key` ([`Int`](/v1/scalars/int))

#### `connection` ([`CurrencyConnection`](/v1/objects/currency-connection))
