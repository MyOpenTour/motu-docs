---
id: delete-location-payload
title: deleteLocationPayload
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type deleteLocationPayload {
  location: Location
}
```

### Fields

#### `location` ([`Location`](/v1/objects/location))
