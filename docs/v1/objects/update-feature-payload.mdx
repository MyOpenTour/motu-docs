---
id: update-feature-payload
title: updateFeaturePayload
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type updateFeaturePayload {
  feature: Feature
}
```

### Fields

#### `feature` ([`Feature`](/v1/objects/feature))
