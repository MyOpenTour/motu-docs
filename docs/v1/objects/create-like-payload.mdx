---
id: create-like-payload
title: createLikePayload
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type createLikePayload {
  like: Like
}
```

### Fields

#### `like` ([`Like`](/v1/objects/like))
