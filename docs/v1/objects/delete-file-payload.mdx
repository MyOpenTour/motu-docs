---
id: delete-file-payload
title: deleteFilePayload
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type deleteFilePayload {
  file: UploadFile
}
```

### Fields

#### `file` ([`UploadFile`](/v1/objects/upload-file))
