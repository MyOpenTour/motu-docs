---
id: create-leg-payload
title: createLegPayload
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type createLegPayload {
  leg: Leg
}
```

### Fields

#### `leg` ([`Leg`](/v1/objects/leg))
