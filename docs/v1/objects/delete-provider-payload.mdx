---
id: delete-provider-payload
title: deleteProviderPayload
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type deleteProviderPayload {
  provider: Provider
}
```

### Fields

#### `provider` ([`Provider`](/v1/objects/provider))
