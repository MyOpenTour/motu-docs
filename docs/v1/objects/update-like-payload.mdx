---
id: update-like-payload
title: updateLikePayload
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type updateLikePayload {
  like: Like
}
```

### Fields

#### `like` ([`Like`](/v1/objects/like))
