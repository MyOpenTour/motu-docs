---
id: delete-leg-payload
title: deleteLegPayload
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type deleteLegPayload {
  leg: Leg
}
```

### Fields

#### `leg` ([`Leg`](/v1/objects/leg))
