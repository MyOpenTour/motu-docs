---
id: create-currency-input
title: createCurrencyInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type createCurrencyInput {
  data: CurrencyInput
}
```

### Fields

#### `data` ([`CurrencyInput`](/v1/inputs/currency-input))
