---
id: delete-coupon-input
title: deleteCouponInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type deleteCouponInput {
  where: InputID
}
```

### Fields

#### `where` ([`InputID`](/v1/inputs/input-id))
