---
id: input-id
title: InputID
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type InputID {
  id: ID!
}
```

### Fields

#### `id` ([`ID!`](/v1/scalars/id))
