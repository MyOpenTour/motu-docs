---
id: update-itinerary-input
title: updateItineraryInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type updateItineraryInput {
  where: InputID
  data: editItineraryInput
}
```

### Fields

#### `where` ([`InputID`](/v1/inputs/input-id))

#### `data` ([`editItineraryInput`](/v1/inputs/edit-itinerary-input))
