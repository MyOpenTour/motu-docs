---
id: create-like-input
title: createLikeInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type createLikeInput {
  data: LikeInput
}
```

### Fields

#### `data` ([`LikeInput`](/v1/inputs/like-input))
