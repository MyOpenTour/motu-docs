---
id: create-location-input
title: createLocationInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type createLocationInput {
  data: LocationInput
}
```

### Fields

#### `data` ([`LocationInput`](/v1/inputs/location-input))
