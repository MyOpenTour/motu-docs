---
id: location
title: location
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
location(
  id: ID!
  publicationState: PublicationState
): Location

```

### Arguments

#### `id` ([`ID!`](/v1/scalars/id))

#### `publicationState` ([`PublicationState`](/v1/enums/publication-state))

### Type

#### [`Location`](/v1/objects/location)
