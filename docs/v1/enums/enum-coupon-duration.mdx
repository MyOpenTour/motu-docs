---
id: enum-coupon-duration
title: ENUM_COUPON_DURATION
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ENUM_COUPON_DURATION {
  once
  repeating
  forever
}
```

### Values

#### `once`

#### `repeating`

#### `forever`
