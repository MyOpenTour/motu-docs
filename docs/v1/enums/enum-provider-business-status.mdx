---
id: enum-provider-business-status
title: ENUM_PROVIDER_BUSINESS_STATUS
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ENUM_PROVIDER_BUSINESS_STATUS {
  OPERATIONAL
  CLOSED_TEMPORARILY
  CLOSED_PERMANENTLY
}
```

### Values

#### `OPERATIONAL`

#### `CLOSED_TEMPORARILY`

#### `CLOSED_PERMANENTLY`
