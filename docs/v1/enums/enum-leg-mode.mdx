---
id: enum-leg-mode
title: ENUM_LEG_MODE
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ENUM_LEG_MODE {
  WALK
  TAXI
}
```

### Values

#### `WALK`

#### `TAXI`
