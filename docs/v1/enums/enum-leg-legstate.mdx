---
id: enum-leg-legstate
title: ENUM_LEG_LEGSTATE
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ENUM_LEG_LEGSTATE {
  START
  PLANNED
  PAID
  ACTIVATED
  CANCELLED
  FINISHED
}
```

### Values

#### `START`

#### `PLANNED`

#### `PAID`

#### `ACTIVATED`

#### `CANCELLED`

#### `FINISHED`
